# ==========================================
# Sentinel Quiz Leaderboard Configuration
# (সেন্টিনেল কুইজ লিডারবোর্ড কনফিগারেশন)
# ==========================================

# --- APP SETTINGS (অ্যাপ সেটিংস) ---
# Valid values: dev, staging, prod
APP__ENVIRONMENT=dev
APP__DEBUG=true
APP__NAME="Sentinel Quiz Leaderboard"

# --- FACEBOOK CONFIG (ফেসবুক কনফিগারেশন) ---
# Path to your JSON cookies file (exported from EditThisCookie or similar)
FACEBOOK__COOKIES_FILE_PATH=./cookies.json
FACEBOOK__USER_DATA_DIR=./user_data
FACEBOOK__MAX_COMMENTS=100

# --- SCRAPER SETTINGS (স্ক্র্যাপার সেটিংস) ---
# Set to 'true' for production (headless mode)
SCRAPER__HEADLESS=false
SCRAPER__SLOW_MO_MIN=50
SCRAPER__SLOW_MO_MAX=200

# --- API SECURITY (API নিরাপত্তা) ---
# [REQUIRED] Strong random string for admin access
# (অ্যাডমিন অ্যাক্সেসের জন্য শক্তিশালী গোপন কি দিন)
API__API_KEY=change_me_to_a_secure_secret_string
API__HOST=0.0.0.0
API__PORT=8000
# Comma separated list of allowed origins
API__ALLOWED_ORIGINS=["http://localhost:3000"]

# --- DATABASE (ডাটাবেস) ---
DATABASE__SQLITE_PATH=leaderboard.db
DATABASE__BACKUP_ENABLED=true

# --- LOGGING (লগিং) ---
# DEBUG, INFO, WARNING, ERROR
LOGGING__LEVEL=INFO
LOGGING__JSON_LOGS=false